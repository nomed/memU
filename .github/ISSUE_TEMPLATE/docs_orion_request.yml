name: Docs Request (Orion)
description: Blank-style docs issue (assign to Orion / `docs.agent.md`)
title: "[DOCS] "
labels: ["documentation"]
body:
  - type: markdown
    attributes:
      value: |
        Free-form docs issue.

        Assign this issue to **Orion** (`.github/agents/docs.agent.md`).

  - type: textarea
    id: details
    attributes:
      label: Details
      description: Describe the docs request
      placeholder: "What should Orion write/update?"
    validations:
      required: true
